module ut.backends.bytecode;


// Bytecode public behaviour tests live in ut.backends.parity so every capable
// backend can share the same fixtures. Keep this module in the suite list as a
// placeholder for future bytecode-specific diagnostics or VM contracts.
